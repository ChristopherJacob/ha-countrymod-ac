[tool.ruff]
target-version = "py313"
line-length = 88

[tool.ruff.lint]
select = ["E", "F", "W", "B", "I", "UP", "SIM", "RUF"]
# Home Assistant entity classes declare _attr_* lists at class level by design.
ignore = ["RUF012"]
